# Implementation Plans

Generated by the improve skill on 2026-06-20. Execute in the order below unless
dependencies say otherwise. Each executor: read the plan fully before starting,
honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Bring mcp-pool reliability/performance closer to mcpproxy-go while preserving per-MCP visibility | P0 | L | — | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale — finding fixed independently or approach abandoned)

## Dependency notes

- Plan 001 is self-contained but should be executed in its internal step order. Step 1 (restore compilation) must happen before any parallel implementation work.
- After Step 1 compiles, work can be split across subagents as described in the plan: routing fallback, downstream lifecycle, discovery coalescing/cache, recovery/observability.

## Findings considered and rejected

- Collapsing all configured MCPs behind one facade like mcpproxy-go direct mode: rejected. The product requirement is to keep each MCP individually visible in the agent config and proxy each one via `mcp-pool proxy <name>`.
- Caching `tools/call`: rejected. Tool calls are user/request-specific, may mutate state, and are not stable upstream discovery data.
- Persisting tool cache to disk: deferred. In-memory cache is enough to address repeated startup/discovery storms in one daemon lifetime and avoids stale persisted tool schemas.
